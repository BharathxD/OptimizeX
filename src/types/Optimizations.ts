export type SafeUserOptimizations = {
    id: string;
    userId: string;
    url: string;
    fileName: string;
    extension: string;
    expired: boolean;
    expiresAt: Date;
    createdAt: string;
}